# Firebase Client

Place Firebase web SDK initialization here when Firebase Authentication and Firestore are implemented.

Keep credentials in `apps/web/.env.local`; only commit placeholder keys in `.env.example`.
